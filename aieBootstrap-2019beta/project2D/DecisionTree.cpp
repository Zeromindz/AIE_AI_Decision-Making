#include "DecisionTree.h"

#include "DebugNew.h"

void DecisionNode::MakeDecision(Agent* _agent, float _deltaTime)
{
	{
		if (m_Value == true)
		{
			// truebranch
		}
		else
		{
			// falsebranch	
		}
	}
}

bool DecisionNode::Update(Agent* _agent, float _deltaTime)
{
	return false;
}
